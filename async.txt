Funcionó:

El archivo se llama main.
Hay un archivo .toml que indica la librería pygame.
Se compila con:
source venv/bin/activate
pygodide build .
zip -r domino-web.zip build/

pygodide serve . (para servir localmente)

---

El main usa asyncio

AL final:

async def main():
    juego = JuegoPygame()
    await juego.ejecutar()

if __name__ == "__main__":
    juego = JuegoPygame()
    asyncio.run(juego.ejecutar())

La def principal es una clase async, tiene esto después del flip:
await asyncio.sleep(1 / 60)  # <--- CEDER CONTROL AL NAVEGADOR